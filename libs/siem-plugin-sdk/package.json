{
  "name": "@osiris/siem-plugin-sdk",
  "version": "1.0.0",
  "description": "OSIRIS-Lab v2 — SIEM Plugin SDK. Shared types and base classes for SIEM connectors.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathPattern=tests/unit"
  },
  "dependencies": {
    "@osiris/shared": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}